import express from 'express';
const supportRouter = express.Router();
import prisma from '../../config/prisma.js';

supportRouter.post("/login", async (req,res)=>{
        
})

export default supportRouter;